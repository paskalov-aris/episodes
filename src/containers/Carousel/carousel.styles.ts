import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
    marginBottom: 24,
  },
  carousel: {
    width: '100%',
  },
  rightIndent: {
    marginRight: 12,
  },
});
